# Variables de entorno para Railway
# Copia este archivo a .env y configura las variables

# Base de datos MySQL (opcional)
# Si no se configura, la app usará SQLite
MYSQL_URI=mysql+mysqlconnector://usuario:contraseña@host:puerto/database

# Puerto (Railway lo configura automáticamente)
PORT=5000

# Configuración JWT
JWT_SECRET_KEY=SML_2025_CHANGE_THIS_IN_PRODUCTION

# Configuración Flask
# development = permite CORS desde cualquier origen (*)
# production = permite múltiples dominios conocidos pero sigue siendo flexible
FLASK_ENV=production
FLASK_DEBUG=False

# Nota sobre CORS:
# La API permite peticiones desde diferentes dominios:
# - En desarrollo: todas las peticiones (*)
# - En producción: todos los dominios pero con configuración de credenciales
# Para restringir a dominios específicos, edita config/cors.py